spring:
  application:
    name: portal
  datasource:
    url:
      jdbc:postgresql://localhost:5432/TravelManagement
    username: postgres
    password: postgre
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true